[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "unirecover"
version = "0.1.0"
description = "Python FFI Bindings for UniRecover Engine"
requires-python = ">=3.7"
dependencies = []
