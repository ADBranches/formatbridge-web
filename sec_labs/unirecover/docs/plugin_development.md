# Forensic Plugin SDK Guide
Extend image discovery bounds by writing isolated analytics handlers:
1. Inherit parameters cleanly from `BaseFileSystemParserPlugin`.
2. Ensure data outputs conform explicitly to the global `CanonicalFileRecord` structure.
