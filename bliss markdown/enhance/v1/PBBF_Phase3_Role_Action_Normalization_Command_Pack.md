# PBBF BLISS — Phase 3 Command Pack (Role-Action Normalization)

## Phase 3 objective
Normalize role actions end-to-end so that **patient**, **provider**, **counselor**, **lactation consultant**, **care coordinator**, and **admin** can perform exactly the workflows the MVP expects — with no missing provider-family access, no accidental overexposure, and no UI route that lands on an unimplemented backend capability. Earlier inspection already showed a mismatch where some provider-family roles are supported in encounters but not consistently in appointment workflows and related pages. citeturn74search1turn76search7turn77search7turn83search1

## Implementation boundary (inspection-first rule preserved)
This command pack only gives **patch commands** for files whose structures have already been inspected in the conversation. For files mentioned in the broader Phase 3 scope whose structures were **not fully inspected yet**, this pack gives **inspection-only commands first** and does **not** prescribe edits until those structures are confirmed. This keeps the phase consistent with the inspect-before-edit rule. citeturn76search7turn77search7turn79search1turn83search1

### Files already inspected and safe to patch in this phase
#### Backend
- `pbbf-api/app/modules/appointments/router.py` citeturn76search7
- `pbbf-api/app/modules/appointments/service.py` citeturn76search7
- `pbbf-api/app/modules/appointments/repository.py` citeturn79search1
- `pbbf-api/app/modules/appointments/schemas.py` citeturn79search1
- `pbbf-api/app/modules/telehealth/router.py` citeturn76search7
- `pbbf-api/app/modules/telehealth/service.py` citeturn76search7
- `pbbf-api/app/modules/encounters/router.py` citeturn76search7
- `pbbf-api/app/modules/encounters/service.py` citeturn76search7
- `pbbf-api/app/modules/screenings/router.py` citeturn76search7
- `pbbf-api/app/modules/screenings/service.py` citeturn76search7
- `pbbf-api/app/modules/referrals/router.py` citeturn76search7
- `pbbf-api/app/modules/referrals/service.py` citeturn76search7

#### Frontend
- `pbbf-telehealth/src/shared/constants/navigation.js` citeturn79search1
- `pbbf-telehealth/src/app/AppRoutes.jsx` citeturn79search1
- `pbbf-telehealth/src/shared/guards/RoleGuard.jsx` *(inspected earlier during role-aware UI work)* citeturn76search7turn79search1
- `pbbf-telehealth/src/pages/provider/Dashboard.jsx` citeturn76search7
- `pbbf-telehealth/src/pages/provider/Notes.jsx` citeturn76search7
- `pbbf-telehealth/src/pages/provider/Referrals.jsx` citeturn76search7
- `pbbf-telehealth/src/modules/appointments/hooks/useAppointments.js` citeturn79search1
- `pbbf-telehealth/src/modules/appointments/services/appointmentsApi.js` citeturn79search1

### Files **not yet safe to patch** without one more inspection pass
- `pbbf-api/app/modules/telehealth/repository.py`
- `pbbf-api/app/common/permissions/object_access.py`
- any role dependency helpers used by auth/users/intake modules

This pack therefore includes **inspection commands only** for those files. citeturn76search7turn77search7turn79search1

---

# 0) Go to repo root and create a backup area

```bash
cd /home/trovas/Downloads/projects/byupw/block3_2026/BLISS/bliss-telehealth
export PHASE3_ROLE_ACTION_BACKUP_DIR="backups/phase3_role_action_$(date +%Y%m%d_%H%M%S)"
mkdir -p "$PHASE3_ROLE_ACTION_BACKUP_DIR"
echo "$PHASE3_ROLE_ACTION_BACKUP_DIR"
```

---

# 1) Confirm the currently inspected files before patching

```bash
cd /home/trovas/Downloads/projects/byupw/block3_2026/BLISS/bliss-telehealth

sed -n '1,320p' pbbf-api/app/modules/appointments/router.py
sed -n '1,520p' pbbf-api/app/modules/appointments/service.py
sed -n '1,360p' pbbf-api/app/modules/appointments/repository.py
sed -n '1,320p' pbbf-api/app/modules/appointments/schemas.py

sed -n '1,360p' pbbf-api/app/modules/telehealth/router.py
sed -n '1,520p' pbbf-api/app/modules/telehealth/service.py

sed -n '1,360p' pbbf-api/app/modules/encounters/router.py
sed -n '1,520p' pbbf-api/app/modules/encounters/service.py

sed -n '1,360p' pbbf-api/app/modules/screenings/router.py
sed -n '1,520p' pbbf-api/app/modules/screenings/service.py

sed -n '1,360p' pbbf-api/app/modules/referrals/router.py
sed -n '1,520p' pbbf-api/app/modules/referrals/service.py

sed -n '1,320p' pbbf-telehealth/src/shared/constants/navigation.js
sed -n '1,340p' pbbf-telehealth/src/app/AppRoutes.jsx
sed -n '1,260p' pbbf-telehealth/src/shared/guards/RoleGuard.jsx
sed -n '1,320p' pbbf-telehealth/src/pages/provider/Dashboard.jsx
sed -n '1,320p' pbbf-telehealth/src/pages/provider/Notes.jsx
sed -n '1,320p' pbbf-telehealth/src/pages/provider/Referrals.jsx
sed -n '1,320p' pbbf-telehealth/src/modules/appointments/hooks/useAppointments.js
sed -n '1,260p' pbbf-telehealth/src/modules/appointments/services/appointmentsApi.js
```

---

# 2) Back up the confirmed patch targets

```bash
cd /home/trovas/Downloads/projects/byupw/block3_2026/BLISS/bliss-telehealth

mkdir -p "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-api/app/modules/appointments"
mkdir -p "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-api/app/modules/telehealth"
mkdir -p "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-api/app/modules/encounters"
mkdir -p "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-api/app/modules/screenings"
mkdir -p "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-api/app/modules/referrals"
mkdir -p "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-telehealth/src/shared/constants"
mkdir -p "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-telehealth/src/shared/guards"
mkdir -p "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-telehealth/src/app"
mkdir -p "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-telehealth/src/pages/provider"
mkdir -p "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-telehealth/src/modules/appointments/hooks"
mkdir -p "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-telehealth/src/modules/appointments/services"

cp pbbf-api/app/modules/appointments/router.py "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-api/app/modules/appointments/router.py"
cp pbbf-api/app/modules/appointments/service.py "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-api/app/modules/appointments/service.py"
cp pbbf-api/app/modules/appointments/repository.py "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-api/app/modules/appointments/repository.py"
cp pbbf-api/app/modules/appointments/schemas.py "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-api/app/modules/appointments/schemas.py"
cp pbbf-api/app/modules/telehealth/router.py "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-api/app/modules/telehealth/router.py"
cp pbbf-api/app/modules/telehealth/service.py "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-api/app/modules/telehealth/service.py"
cp pbbf-api/app/modules/encounters/router.py "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-api/app/modules/encounters/router.py"
cp pbbf-api/app/modules/encounters/service.py "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-api/app/modules/encounters/service.py"
cp pbbf-api/app/modules/screenings/router.py "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-api/app/modules/screenings/router.py"
cp pbbf-api/app/modules/screenings/service.py "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-api/app/modules/screenings/service.py"
cp pbbf-api/app/modules/referrals/router.py "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-api/app/modules/referrals/router.py"
cp pbbf-api/app/modules/referrals/service.py "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-api/app/modules/referrals/service.py"

cp pbbf-telehealth/src/shared/constants/navigation.js "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-telehealth/src/shared/constants/navigation.js"
cp pbbf-telehealth/src/app/AppRoutes.jsx "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-telehealth/src/app/AppRoutes.jsx"
cp pbbf-telehealth/src/shared/guards/RoleGuard.jsx "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-telehealth/src/shared/guards/RoleGuard.jsx"
cp pbbf-telehealth/src/pages/provider/Dashboard.jsx "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-telehealth/src/pages/provider/Dashboard.jsx"
cp pbbf-telehealth/src/pages/provider/Notes.jsx "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-telehealth/src/pages/provider/Notes.jsx"
cp pbbf-telehealth/src/pages/provider/Referrals.jsx "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-telehealth/src/pages/provider/Referrals.jsx"
cp pbbf-telehealth/src/modules/appointments/hooks/useAppointments.js "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-telehealth/src/modules/appointments/hooks/useAppointments.js"
cp pbbf-telehealth/src/modules/appointments/services/appointmentsApi.js "$PHASE3_ROLE_ACTION_BACKUP_DIR/pbbf-telehealth/src/modules/appointments/services/appointmentsApi.js"
```

---

# 3) Inspect the still-unknown authorization files **before** any edit

## Why this step exists
Role-action normalization depends heavily on object-access and shared auth dependency behavior. These files were named in the phase scope but were **not fully inspected yet**, so do this inspection before touching them. Do **not** edit them until you paste the outputs. citeturn76search7turn77search7turn79search1

```bash
cd /home/trovas/Downloads/projects/byupw/block3_2026/BLISS/bliss-telehealth

echo "===== OBJECT ACCESS ====="
sed -n '1,360p' pbbf-api/app/common/permissions/object_access.py

echo

echo "===== TELEHEALTH REPOSITORY ====="
sed -n '1,360p' pbbf-api/app/modules/telehealth/repository.py

echo

echo "===== AUTH DEPENDENCIES ====="
sed -n '1,260p' pbbf-api/app/modules/auth/dependencies.py

echo

echo "===== USERS DEPENDENCIES ====="
sed -n '1,260p' pbbf-api/app/modules/users/dependencies.py
```

### Stop point
Do not patch those files yet. The rest of this pack patches only the already-inspected structures.

---

# 4) Patch backend appointments service

## What this changes
- treats `counselor` and `lactation_consultant` as provider-family roles for appointment listing
- allows provider-family users to view appointments through the same provider-profile path used by `provider`
- leaves create/reschedule/cancel ownership constraints intact for patients
- keeps admin and care coordinator operational visibility intact

This addresses the known mismatch where some provider-family roles were supported in encounters but not consistently in appointment workflows. citeturn76search7turn77search7turn83search1

```bash
cd /home/trovas/Downloads/projects/byupw/block3_2026/BLISS/bliss-telehealth
python3 -c 'from pathlib import Path; Path("pbbf-api/app/modules/appointments/service.py").write_text("""from __future__ import annotations\n\nfrom fastapi import HTTPException, status\nfrom app.common.permissions.object_access import patient_owns_patient_id, provider_profile_id\nfrom sqlalchemy.orm import Session\nfrom app.modules.appointments.repository import AppointmentsRepository\nfrom app.modules.appointments.schemas import (\n    AppointmentAssignProviderRequest,\n    AppointmentCancelRequest,\n    AppointmentCreateRequest,\n    AppointmentRescheduleRequest,\n)\n\nPROVIDER_FAMILY_ROLES = {\"provider\", \"counselor\", \"lactation_consultant\"}\nADMIN_COORDINATOR_ROLES = {\"admin\", \"care_coordinator\"}\n\n\nclass AppointmentsService:\n    def __init__(self, db: Session) -> None:\n        self.db = db\n        self.repository = AppointmentsRepository(db)\n\n    @staticmethod\n    def _role_name(user) -> str:\n        role = getattr(getattr(user, \"role\", None), \"name\", \"\") or getattr(user, \"role\", \"\")\n        return str(role).strip().lower()\n\n    @staticmethod\n    def _serialize(appointment) -> dict:\n        patient_name = getattr(getattr(appointment, \"patient\", None), \"user\", None)\n        provider_user = getattr(getattr(appointment, \"provider\", None), \"user\", None)\n\n        return {\n            \"id\": appointment.id,\n            \"patient_id\": appointment.patient_id,\n            \"provider_id\": appointment.provider_id,\n            \"service_type\": appointment.appointment_type,\n            \"reason\": appointment.reason,\n            \"timezone\": appointment.timezone_name,\n            \"start_at\": appointment.scheduled_start,\n            \"end_at\": appointment.scheduled_end,\n            \"status\": appointment.status,\n            \"cancellation_reason\": appointment.cancellation_reason,\n            \"patient_name\": getattr(patient_name, \"full_name\", None),\n            \"provider_name\": getattr(provider_user, \"full_name\", None),\n        }\n\n    def _require_patient_profile(self, current_user):\n        role_name = self._role_name(current_user)\n        patient_profile = getattr(current_user, \"patient_profile\", None)\n        if role_name != \"patient\" or patient_profile is None:\n            raise HTTPException(\n                status_code=status.HTTP_403_FORBIDDEN,\n                detail=\"Only patients can perform this action.\",\n            )\n        return patient_profile\n\n    def _require_provider_family_profile(self, current_user):\n        role_name = self._role_name(current_user)\n        provider_profile = getattr(current_user, \"provider_profile\", None)\n        if role_name not in PROVIDER_FAMILY_ROLES or provider_profile is None:\n            raise HTTPException(\n                status_code=status.HTTP_403_FORBIDDEN,\n                detail=\"Only provider-family users can perform this action.\",\n            )\n        return provider_profile\n\n    def _require_admin(self, current_user):\n        role_name = self._role_name(current_user)\n        if role_name != \"admin\":\n            raise HTTPException(\n                status_code=status.HTTP_403_FORBIDDEN,\n                detail=\"Only admins can perform this action.\",\n            )\n\n    def create_appointment(self, current_user, payload: AppointmentCreateRequest) -> dict:\n        patient_profile = self._require_patient_profile(current_user)\n        provider = self.repository.get_provider_by_id(payload.provider_id) if payload.provider_id else None\n\n        if payload.provider_id and provider is None:\n            raise HTTPException(\n                status_code=status.HTTP_404_NOT_FOUND,\n                detail=\"Provider not found.\",\n            )\n\n        if payload.end_at <= payload.start_at:\n            raise HTTPException(\n                status_code=status.HTTP_400_BAD_REQUEST,\n                detail=\"Appointment end time must be after start time.\",\n            )\n\n        if payload.provider_id and self.repository.has_provider_conflict(\n            provider_id=payload.provider_id,\n            start_at=payload.start_at,\n            end_at=payload.end_at,\n        ):\n            raise HTTPException(\n                status_code=status.HTTP_400_BAD_REQUEST,\n                detail=\"Provider has a conflicting appointment.\",\n            )\n\n        appointment = self.repository.create_appointment(\n            patient_id=patient_profile.id,\n            provider_id=payload.provider_id,\n            appointment_type=payload.service_type,\n            reason=payload.reason,\n            timezone_name=payload.timezone,\n            scheduled_start=payload.start_at,\n            scheduled_end=payload.end_at,\n            status=\"booked\" if payload.provider_id else \"requested\",\n        )\n        return self._serialize(appointment)\n\n    def cancel_appointment(self, appointment_id: int, current_user, payload: AppointmentCancelRequest) -> dict:\n        self._require_patient_profile(current_user)\n        appointment = self.repository.get_appointment_by_id(appointment_id)\n        if appointment is None:\n            raise HTTPException(\n                status_code=status.HTTP_404_NOT_FOUND,\n                detail=\"Appointment not found.\",\n            )\n\n        if not patient_owns_patient_id(current_user, appointment.patient_id):\n            raise HTTPException(\n                status_code=status.HTTP_403_FORBIDDEN,\n                detail=\"You can only cancel your own appointments.\",\n            )\n\n        if appointment.status == \"cancelled\":\n            raise HTTPException(\n                status_code=status.HTTP_400_BAD_REQUEST,\n                detail=\"Appointment cannot be cancelled again.\",\n            )\n\n        appointment.status = \"cancelled\"\n        appointment.cancellation_reason = payload.reason\n        appointment = self.repository.save(appointment)\n        return self._serialize(appointment)\n\n    def reschedule_appointment(self, appointment_id: int, current_user, payload: AppointmentRescheduleRequest) -> dict:\n        self._require_patient_profile(current_user)\n        appointment = self.repository.get_appointment_by_id(appointment_id)\n        if appointment is None:\n            raise HTTPException(\n                status_code=status.HTTP_404_NOT_FOUND,\n                detail=\"Appointment not found.\",\n            )\n\n        if not patient_owns_patient_id(current_user, appointment.patient_id):\n            raise HTTPException(\n                status_code=status.HTTP_403_FORBIDDEN,\n                detail=\"You can only reschedule your own appointments.\",\n            )\n\n        if payload.end_at <= payload.start_at:\n            raise HTTPException(\n                status_code=status.HTTP_400_BAD_REQUEST,\n                detail=\"Appointment end time must be after start time.\",\n            )\n\n        if appointment.provider_id is not None and self.repository.has_provider_conflict(\n            provider_id=appointment.provider_id,\n            start_at=payload.start_at,\n            end_at=payload.end_at,\n            exclude_appointment_id=appointment.id,\n        ):\n            raise HTTPException(\n                status_code=status.HTTP_400_BAD_REQUEST,\n                detail=\"Provider has a conflicting appointment.\",\n            )\n\n        appointment.scheduled_start = payload.start_at\n        appointment.scheduled_end = payload.end_at\n        appointment.timezone_name = payload.timezone\n        if payload.reason:\n            appointment.reason = payload.reason\n        appointment.status = \"rescheduled\"\n        appointment = self.repository.save(appointment)\n        return self._serialize(appointment)\n\n    def assign_provider(self, appointment_id: int, current_user, payload: AppointmentAssignProviderRequest) -> dict:\n        self._require_admin(current_user)\n        appointment = self.repository.get_appointment_by_id(appointment_id)\n        if appointment is None:\n            raise HTTPException(\n                status_code=status.HTTP_404_NOT_FOUND,\n                detail=\"Appointment not found.\",\n            )\n\n        provider = self.repository.get_provider_by_id(payload.provider_id) if payload.provider_id else None\n        if payload.provider_id and provider is None:\n            raise HTTPException(\n                status_code=status.HTTP_404_NOT_FOUND,\n                detail=\"Provider not found.\",\n            )\n\n        if payload.provider_id and self.repository.has_provider_conflict(\n            provider_id=payload.provider_id,\n            start_at=appointment.scheduled_start,\n            end_at=appointment.scheduled_end,\n            exclude_appointment_id=appointment.id,\n        ):\n            raise HTTPException(\n                status_code=status.HTTP_400_BAD_REQUEST,\n                detail=\"Provider has a conflicting appointment.\",\n            )\n\n        appointment.provider_id = payload.provider_id\n        appointment = self.repository.save(appointment)\n        return self._serialize(appointment)\n\n    def list_provider_appointments(self, current_user) -> dict:\n        provider_profile = self._require_provider_family_profile(current_user)\n        appointments = self.repository.list_for_provider(provider_profile.id)\n        items = [self._serialize(appointment) for appointment in appointments]\n        return {\"items\": items, \"total\": len(items)}\n\n    def list_appointments(self, current_user) -> dict:\n        role_name = self._role_name(current_user)\n\n        if role_name == \"patient\":\n            patient_profile = self._require_patient_profile(current_user)\n            appointments = self.repository.list_for_patient(patient_profile.id)\n        elif role_name in PROVIDER_FAMILY_ROLES:\n            provider_profile = self._require_provider_family_profile(current_user)\n            appointments = self.repository.list_for_provider(provider_profile.id)\n        elif role_name in ADMIN_COORDINATOR_ROLES:\n            appointments = self.repository.list_all()\n        else:\n            raise HTTPException(\n                status_code=status.HTTP_403_FORBIDDEN,\n                detail=\"You are not allowed to list appointments.\",\n            )\n\n        items = [self._serialize(appointment) for appointment in appointments]\n        return {\"items\": items, \"total\": len(items)}\n""", encoding="utf-8")'
```

---

# 5) Patch backend appointments repository

## What this changes
- ensures list queries join related patient/provider records so provider-family dashboards/pages get `patient_name` and `provider_name` consistently from one API shape

```bash
cd /home/trovas/Downloads/projects/byupw/block3_2026/BLISS/bliss-telehealth
python3 -c 'from pathlib import Path; Path("pbbf-api/app/modules/appointments/repository.py").write_text("""from __future__ import annotations\n\nfrom datetime import datetime\nfrom typing import Optional\n\nfrom sqlalchemy import select\nfrom sqlalchemy.orm import Session, joinedload\n\nfrom app.db.models.appointment import Appointment\nfrom app.db.models.provider import Provider\n\n\nACTIVE_APPOINTMENT_STATUSES = (\"booked\", \"rescheduled\")\n\n\nclass AppointmentsRepository:\n    def __init__(self, db: Session) -> None:\n        self.db = db\n\n    def get_provider_by_id(self, provider_id: int) -> Optional[Provider]:\n        stmt = select(Provider).where(Provider.id == provider_id)\n        return self.db.execute(stmt).scalar_one_or_none()\n\n    def get_appointment_by_id(self, appointment_id: int) -> Optional[Appointment]:\n        stmt = (\n            select(Appointment)\n            .options(\n                joinedload(Appointment.patient).joinedload(\"user\"),\n                joinedload(Appointment.provider).joinedload(\"user\"),\n            )\n            .where(Appointment.id == appointment_id)\n        )\n        return self.db.execute(stmt).scalar_one_or_none()\n\n    def has_provider_conflict(\n        self,\n        *,\n        provider_id: int,\n        start_at: datetime,\n        end_at: datetime,\n        exclude_appointment_id: int | None = None,\n    ) -> bool:\n        stmt = select(Appointment).where(\n            Appointment.provider_id == provider_id,\n            Appointment.status.in_(ACTIVE_APPOINTMENT_STATUSES),\n            Appointment.scheduled_start < end_at,\n            Appointment.scheduled_end > start_at,\n        )\n\n        if exclude_appointment_id is not None:\n            stmt = stmt.where(Appointment.id != exclude_appointment_id)\n\n        existing = self.db.execute(stmt).scalar_one_or_none()\n        return existing is not None\n\n    def create_appointment(\n        self,\n        *,\n        patient_id: int,\n        provider_id: int | None,\n        appointment_type: str,\n        reason: str | None,\n        timezone_name: str | None,\n        scheduled_start: datetime,\n        scheduled_end: datetime,\n        status: str = \"booked\",\n    ) -> Appointment:\n        appointment = Appointment(\n            patient_id=patient_id,\n            provider_id=provider_id,\n            appointment_type=appointment_type,\n            reason=reason,\n            timezone_name=timezone_name,\n            scheduled_start=scheduled_start,\n            scheduled_end=scheduled_end,\n            status=status,\n        )\n        self.db.add(appointment)\n        self.db.commit()\n        self.db.refresh(appointment)\n        return self.get_appointment_by_id(appointment.id)\n\n    def save(self, appointment: Appointment) -> Appointment:\n        self.db.add(appointment)\n        self.db.commit()\n        self.db.refresh(appointment)\n        return self.get_appointment_by_id(appointment.id)\n\n    def list_for_provider(self, provider_id: int) -> list[Appointment]:\n        stmt = (\n            select(Appointment)\n            .options(\n                joinedload(Appointment.patient).joinedload(\"user\"),\n                joinedload(Appointment.provider).joinedload(\"user\"),\n            )\n            .where(Appointment.provider_id == provider_id)\n            .order_by(Appointment.scheduled_start.asc(), Appointment.id.asc())\n        )\n        return list(self.db.execute(stmt).scalars().all())\n\n    def list_for_patient(self, patient_id: int) -> list[Appointment]:\n        stmt = (\n            select(Appointment)\n            .options(\n                joinedload(Appointment.patient).joinedload(\"user\"),\n                joinedload(Appointment.provider).joinedload(\"user\"),\n            )\n            .where(Appointment.patient_id == patient_id)\n            .order_by(Appointment.scheduled_start.asc(), Appointment.id.asc())\n        )\n        return list(self.db.execute(stmt).scalars().all())\n\n    def list_all(self) -> list[Appointment]:\n        stmt = (\n            select(Appointment)\n            .options(\n                joinedload(Appointment.patient).joinedload(\"user\"),\n                joinedload(Appointment.provider).joinedload(\"user\"),\n            )\n            .order_by(Appointment.scheduled_start.asc(), Appointment.id.asc())\n        )\n        return list(self.db.execute(stmt).scalars().all())\n""", encoding="utf-8")'
```

---

# 6) Patch backend telehealth service

## What this changes
- normalizes provider-family role handling so `counselor` and `lactation_consultant` are treated consistently with `provider` where appropriate
- keeps admin/care coordinator operational access intact

```bash
cd /home/trovas/Downloads/projects/byupw/block3_2026/BLISS/bliss-telehealth
python3 -c 'from pathlib import Path; Path("pbbf-api/app/modules/telehealth/service.py").write_text("""from __future__ import annotations\n\nfrom app.common.errors.http_exceptions import ForbiddenException, NotFoundException, ValidationException\nfrom app.common.permissions.object_access import can_view_appointment, patient_profile_id, provider_profile_id\nfrom app.modules.telehealth.providers import get_virtual_visit_provider\nfrom app.modules.telehealth.repository import TelehealthRepository\n\nPROVIDER_FAMILY_ROLES = {\"provider\", \"counselor\", \"lactation_consultant\"}\nADMIN_COORDINATOR_ROLES = {\"admin\", \"care_coordinator\"}\n\nALLOWED_STATUS_TRANSITIONS: dict[str, set[str]] = {\n    \"scheduled\": {\"waiting\", \"in_progress\", \"ended\", \"no_show\"},\n    \"waiting\": {\"in_progress\", \"ended\", \"no_show\"},\n    \"in_progress\": {\"ended\"},\n    \"ended\": set(),\n    \"no_show\": set(),\n}\n\n\nclass TelehealthService:\n    def __init__(self, repository: TelehealthRepository) -> None:\n        self.repository = repository\n        self.provider = get_virtual_visit_provider()\n\n    @staticmethod\n    def _role_name(user) -> str:\n        role = getattr(user, \"role\", None)\n        if isinstance(role, str):\n            return role.strip().lower()\n        return (getattr(role, \"name\", \"\") or \"\").strip().lower()\n\n    def ensure_session_for_appointment(self, *, appointment_id: int, current_user) -> object:\n        appointment = self.repository.get_appointment(appointment_id)\n        if appointment is None:\n            raise NotFoundException(\"Appointment not found.\")\n\n        role = self._role_name(current_user)\n        if role not in PROVIDER_FAMILY_ROLES | ADMIN_COORDINATOR_ROLES:\n            raise ForbiddenException(\"You are not allowed to create session metadata for this appointment.\")\n\n        existing = self.repository.get_session_by_appointment_id(appointment_id)\n        if existing is not None:\n            return existing\n\n        generated = self.provider.create_session(appointment_id)\n        return self.repository.create_session(\n            appointment_id=appointment_id,\n            external_meeting_id=generated.external_meeting_id,\n            join_url=generated.patient_join_url,\n            provider_join_url=generated.provider_join_url,\n        )\n\n    def get_session(self, *, session_id: int, current_user):\n        session = self.repository.get_session_by_id(session_id)\n        if session is None:\n            raise NotFoundException(\"Telehealth session not found.\")\n\n        appointment = self.repository.get_appointment(session.appointment_id)\n        if appointment is None:\n            raise NotFoundException(\"Linked appointment not found.\")\n\n        if not can_view_appointment(current_user, appointment):\n            raise ForbiddenException(\"You are not allowed to view this telehealth session.\")\n\n        return session\n\n    def join_session(self, *, session_id: int, current_user) -> tuple[object, str, str]:\n        session = self.repository.get_session_by_id(session_id)\n        if session is None:\n            raise NotFoundException(\"Telehealth session not found.\")\n\n        appointment = self.repository.get_appointment(session.appointment_id)\n        if appointment is None:\n            raise NotFoundException(\"Linked appointment not found.\")\n\n        role = self._role_name(current_user)\n        actor: str | None = None\n        join_url = session.join_url\n\n        if role == \"patient\" and appointment.patient_id == patient_profile_id(current_user):\n            actor = \"patient\"\n            join_url = session.join_url\n        elif role in PROVIDER_FAMILY_ROLES and appointment.provider_id == provider_profile_id(current_user):\n            actor = \"provider\"\n            join_url = session.provider_join_url or session.join_url\n        elif role in ADMIN_COORDINATOR_ROLES:\n            actor = \"provider\"\n            join_url = session.provider_join_url or session.join_url\n        else:\n            raise ForbiddenException(\"You are not allowed to join this session.\")\n\n        session = self.repository.touch_join(session, actor=actor)\n        return session, actor, join_url\n\n    def update_status(self, *, session_id: int, new_status: str, current_user):\n        session = self.repository.get_session_by_id(session_id)\n        if session is None:\n            raise NotFoundException(\"Telehealth session not found.\")\n\n        appointment = self.repository.get_appointment(session.appointment_id)\n        if appointment is None:\n            raise NotFoundException(\"Linked appointment not found.\")\n\n        role = self._role_name(current_user)\n        if role not in ADMIN_COORDINATOR_ROLES and not (\n            role in PROVIDER_FAMILY_ROLES and appointment.provider_id == provider_profile_id(current_user)\n        ):\n            raise ForbiddenException(\"You are not allowed to change session status.\")\n\n        current_status = session.session_status\n        allowed = ALLOWED_STATUS_TRANSITIONS.get(current_status, set())\n        if new_status not in allowed and new_status != current_status:\n            raise ValidationException(\n                f\"Invalid session status transition from '{current_status}' to '{new_status}'.\"\n            )\n\n        return self.repository.update_status(session, new_status=new_status)\n\n    def list_for_current_user(self, current_user):\n        role = self._role_name(current_user)\n        if role == \"patient\":\n            subject_id = patient_profile_id(current_user)\n        elif role in PROVIDER_FAMILY_ROLES:\n            subject_id = provider_profile_id(current_user)\n        else:\n            subject_id = getattr(current_user, \"id\", None)\n\n        return self.repository.list_for_user(subject_id=subject_id, role=role)\n""", encoding="utf-8")'
```

---

# 7) Patch backend encounters service

## What this changes
- keeps provider-family edit/finalize support explicit and consistent
- no role expansion beyond the currently intended provider-family + admin scope

```bash
cd /home/trovas/Downloads/projects/byupw/block3_2026/BLISS/bliss-telehealth
python3 -c 'from pathlib import Path; Path("pbbf-api/app/modules/encounters/service.py").write_text("""from __future__ import annotations\n\nfrom app.common.errors.http_exceptions import ForbiddenException, NotFoundException, ValidationException\nfrom app.common.permissions.object_access import can_edit_encounter, can_view_encounter, provider_profile_id\nfrom app.modules.encounters.repository import EncounterRepository\nfrom app.modules.encounters.templates import get_note_template\n\nPROVIDER_FAMILY_ROLES = {\"provider\", \"counselor\", \"lactation_consultant\"}\nEDIT_ROLES = PROVIDER_FAMILY_ROLES | {\"admin\"}\n\n\nclass EncounterService:\n    def __init__(self, repository: EncounterRepository) -> None:\n        self.repository = repository\n\n    @staticmethod\n    def _role_name(user) -> str:\n        role = getattr(user, \"role\", None)\n        if isinstance(role, str):\n            return role.strip().lower()\n        return (getattr(role, \"name\", \"\") or \"\").strip().lower()\n\n    def create_for_appointment(self, *, appointment_id: int, template_name: str, current_user):\n        appointment = self.repository.get_appointment(appointment_id)\n        if appointment is None:\n            raise NotFoundException(\"Appointment not found.\")\n\n        role = self._role_name(current_user)\n        if role not in EDIT_ROLES:\n            raise ForbiddenException(\"You are not allowed to create encounter documentation.\")\n\n        if role != \"admin\" and appointment.provider_id != provider_profile_id(current_user):\n            raise ForbiddenException(\"You are not assigned to this appointment.\")\n\n        existing = self.repository.get_by_appointment_id(appointment_id)\n        if existing is not None:\n            return existing\n\n        template = get_note_template(template_name)\n        return self.repository.create(\n            appointment_id=appointment.id,\n            patient_id=appointment.patient_id,\n            provider_id=appointment.provider_id,\n            template_name=template_name,\n            subjective=template.get(\"subjective\"),\n            objective=template.get(\"objective\"),\n            assessment=template.get(\"assessment\"),\n            plan=template.get(\"plan\"),\n            follow_up_plan=template.get(\"follow_up_plan\"),\n        )\n\n    def get_by_appointment_id(self, *, appointment_id: int, current_user):\n        encounter = self.repository.get_by_appointment_id(appointment_id)\n        if encounter is None:\n            raise NotFoundException(\"Encounter not found.\")\n        return self.get_by_id(encounter_id=encounter.id, current_user=current_user)\n\n    def get_by_id(self, *, encounter_id: int, current_user):\n        encounter = self.repository.get_by_id(encounter_id)\n        if encounter is None:\n            raise NotFoundException(\"Encounter not found.\")\n        if not can_view_encounter(current_user, encounter):\n            raise ForbiddenException(\"You are not allowed to view this encounter.\")\n        return encounter\n\n    def save_draft(self, *, encounter_id: int, payload, current_user):\n        encounter = self.repository.get_by_id(encounter_id)\n        if encounter is None:\n            raise NotFoundException(\"Encounter not found.\")\n\n        role = self._role_name(current_user)\n        if role not in EDIT_ROLES:\n            raise ForbiddenException(\"You are not allowed to edit encounter documentation.\")\n\n        if role != \"admin\" and not can_edit_encounter(current_user, encounter):\n            raise ForbiddenException(\"You are not allowed to edit this encounter.\")\n\n        if encounter.status == \"finalized\":\n            raise ValidationException(\"Finalized encounters cannot be modified.\")\n\n        return self.repository.update_draft(\n            encounter,\n            subjective=payload.subjective,\n            objective=payload.objective,\n            assessment=payload.assessment,\n            plan=payload.plan,\n            note_text=payload.note_text,\n            follow_up_plan=payload.follow_up_plan,\n        )\n\n    def finalize(self, *, encounter_id: int, current_user):\n        encounter = self.repository.get_by_id(encounter_id)\n        if encounter is None:\n            raise NotFoundException(\"Encounter not found.\")\n\n        role = self._role_name(current_user)\n        user_id = getattr(current_user, \"id\", None)\n        if role not in EDIT_ROLES:\n            raise ForbiddenException(\"You are not allowed to finalize encounter documentation.\")\n\n        if role != \"admin\" and not can_edit_encounter(current_user, encounter):\n            raise ForbiddenException(\"You are not allowed to finalize this encounter.\")\n\n        required_fields = [encounter.assessment, encounter.plan, encounter.follow_up_plan]\n        if any(not value or not str(value).strip() for value in required_fields):\n            raise ValidationException(\"Assessment, plan, and follow-up plan are required before finalizing a note.\")\n\n        if encounter.status == \"finalized\":\n            return encounter\n\n        return self.repository.finalize(encounter, finalized_by_id=user_id)\n""", encoding="utf-8")'
```

---

# 8) Patch backend screenings service

## What this changes
- normalizes provider-family screening history access to include counselor and lactation consultant consistently where provider-family access exists
- preserves admin/care coordinator access and patient self-history boundaries

```bash
cd /home/trovas/Downloads/projects/byupw/block3_2026/BLISS/bliss-telehealth
python3 -c 'from pathlib import Path; Path("pbbf-api/app/modules/screenings/service.py").write_text("""from __future__ import annotations\n\nfrom fastapi import HTTPException, status\nfrom sqlalchemy.orm import Session\n\nfrom app.db.models.screening import Screening\nfrom app.modules.screenings.constants import CARE_TEAM_REVIEW_ROLES\nfrom app.modules.screenings.repository import ScreeningModuleRepository\nfrom app.modules.screenings.scoring import EPDSEngine, EPDSValidationError\nfrom app.modules.screenings.schemas import EPDSSubmissionCreate, ScreeningReviewUpdate\n\nPROVIDER_FAMILY_ROLES = {\"provider\", \"counselor\", \"lactation_consultant\"}\nSTAFF_SUBMIT_ROLES = {\"admin\", \"care_coordinator\"} | PROVIDER_FAMILY_ROLES\n\n\nclass ScreeningService:\n    def __init__(self, db: Session):\n        self.db = db\n        self.repository = ScreeningModuleRepository(db)\n\n    @staticmethod\n    def _role_name(user) -> str:\n        return (getattr(getattr(user, \"role\", None), \"name\", \"\") or getattr(user, \"role\", \"\") or \"\").strip().lower()\n\n    def _resolve_patient_for_submission(self, *, current_user, payload: EPDSSubmissionCreate) -> int:\n        role_name = self._role_name(current_user)\n        if role_name == \"patient\":\n            patient = self.repository.get_patient_by_user_id(current_user.id)\n            if patient is None:\n                raise HTTPException(\n                    status_code=status.HTTP_404_NOT_FOUND,\n                    detail=\"Patient profile was not found for the authenticated user.\",\n                )\n            return patient.id\n\n        if role_name in STAFF_SUBMIT_ROLES:\n            if payload.patient_id is None:\n                raise HTTPException(\n                    status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,\n                    detail=\"patient_id is required for staff-submitted screenings.\",\n                )\n\n            patient = self.repository.get_patient_by_id(payload.patient_id)\n            if patient is None:\n                raise HTTPException(\n                    status_code=status.HTTP_404_NOT_FOUND,\n                    detail=\"Patient was not found.\",\n                )\n\n            if role_name in PROVIDER_FAMILY_ROLES:\n                provider = self.repository.get_provider_by_user_id(current_user.id)\n                if provider is None:\n                    raise HTTPException(\n                        status_code=status.HTTP_404_NOT_FOUND,\n                        detail=\"Provider profile was not found for the authenticated user.\",\n                    )\n\n                if not self.repository.has_provider_access_to_patient(provider_id=provider.id, patient_id=patient.id):\n                    raise HTTPException(\n                        status_code=status.HTTP_403_FORBIDDEN,\n                        detail=\"Provider-family user does not have access to submit screening for this patient.\",\n                    )\n\n            return patient.id\n\n        raise HTTPException(\n            status_code=status.HTTP_403_FORBIDDEN,\n            detail=\"This role is not allowed to submit screenings.\",\n        )\n\n    def _validate_encounter_patient_link(self, *, patient_id: int, encounter_id: int | None) -> None:\n        if encounter_id is None:\n            return\n\n        encounter = self.repository.get_encounter_by_id(encounter_id)\n        if encounter is None:\n            raise HTTPException(\n                status_code=status.HTTP_404_NOT_FOUND,\n                detail=\"Encounter was not found.\",\n            )\n\n        if encounter.patient_id != patient_id:\n            raise HTTPException(\n                status_code=status.HTTP_400_BAD_REQUEST,\n                detail=\"Encounter does not belong to the supplied patient.\",\n            )\n\n    def submit_epds(self, *, current_user, payload: EPDSSubmissionCreate) -> Screening:\n        patient_id = self._resolve_patient_for_submission(current_user=current_user, payload=payload)\n        self._validate_encounter_patient_link(patient_id=patient_id, encounter_id=payload.encounter_id)\n\n        try:\n            evaluation = EPDSEngine.evaluate(payload.answers.model_dump())\n        except EPDSValidationError as exc:\n            raise HTTPException(\n                status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,\n                detail=str(exc),\n            ) from exc\n\n        answers_json = {\n            \"format\": \"epds_standard_v1\",\n            \"recall_period\": \"past_7_days\",\n            \"raw_answers\": evaluation.raw_answers,\n            \"scored_answers\": evaluation.scored_answers,\n            \"item_10\": {\n                \"score\": evaluation.item_10_score,\n                \"requires_review\": evaluation.item_10_requires_review,\n            },\n            \"scoring_rules\": {\n                \"normal_scored_questions\": [1, 2, 4],\n                \"reverse_scored_questions\": [3, 5, 6, 7, 8, 9, 10],\n                \"maximum_score\": 30,\n            },\n        }\n\n        return self.repository.create_epds(\n            patient_id=patient_id,\n            created_by_user_id=current_user.id,\n            encounter_id=payload.encounter_id,\n            answers_json=answers_json,\n            score=evaluation.total_score,\n            severity_band=evaluation.severity_band,\n            interpretation=evaluation.interpretation,\n            critical_flag=evaluation.critical_flag,\n        )\n\n    def list_history_for_current_patient(self, *, current_user) -> list[Screening]:\n        patient = self.repository.get_patient_by_user_id(current_user.id)\n        if patient is None:\n            raise HTTPException(\n                status_code=status.HTTP_404_NOT_FOUND,\n                detail=\"Patient profile was not found for the authenticated user.\",\n            )\n        return self.repository.list_for_patient(patient.id)\n\n    def list_history_for_patient(self, *, current_user, patient_id: int) -> list[Screening]:\n        role_name = self._role_name(current_user)\n\n        if role_name == \"patient\":\n            patient = self.repository.get_patient_by_user_id(current_user.id)\n            if patient is None or patient.id != patient_id:\n                raise HTTPException(\n                    status_code=status.HTTP_403_FORBIDDEN,\n                    detail=\"Patients may only view their own screening history.\",\n                )\n            return self.repository.list_for_patient(patient_id)\n\n        if role_name in {\"admin\", \"care_coordinator\"}:\n            return self.repository.list_for_patient(patient_id)\n\n        if role_name in PROVIDER_FAMILY_ROLES:\n            provider = self.repository.get_provider_by_user_id(current_user.id)\n            if provider is None:\n                raise HTTPException(\n                    status_code=status.HTTP_404_NOT_FOUND,\n                    detail=\"Provider profile was not found for the authenticated user.\",\n                )\n            if not self.repository.has_provider_access_to_patient(provider_id=provider.id, patient_id=patient_id):\n                raise HTTPException(\n                    status_code=status.HTTP_403_FORBIDDEN,\n                    detail=\"Provider-family user does not have access to this patient's screening history.\",\n                )\n            return self.repository.list_for_patient(patient_id)\n\n        raise HTTPException(\n            status_code=status.HTTP_403_FORBIDDEN,\n            detail=\"This role is not allowed to view screening history.\",\n        )\n\n    def get_screening_detail(self, *, current_user, screening_id: int) -> Screening:\n        screening = self.repository.get_screening_by_id(screening_id)\n        if screening is None:\n            raise HTTPException(\n                status_code=status.HTTP_404_NOT_FOUND,\n                detail=\"Screening was not found.\",\n            )\n\n        allowed_history = self.list_history_for_patient(current_user=current_user, patient_id=screening.patient_id)\n        allowed_ids = {item.id for item in allowed_history}\n        if screening.id not in allowed_ids:\n            raise HTTPException(\n                status_code=status.HTTP_403_FORBIDDEN,\n                detail=\"You do not have access to this screening.\",\n            )\n        return screening\n\n    def review_screening(self, *, current_user, screening_id: int, payload: ScreeningReviewUpdate) -> Screening:\n        role_name = self._role_name(current_user)\n        if role_name not in CARE_TEAM_REVIEW_ROLES:\n            raise HTTPException(\n                status_code=status.HTTP_403_FORBIDDEN,\n                detail=\"Only staff users may review a screening.\",\n            )\n        screening = self.get_screening_detail(current_user=current_user, screening_id=screening_id)\n        return self.repository.update_review(\n            screening,\n            review_notes=payload.review_notes,\n            status=payload.status,\n        )\n\n    def ensure_role_can_review(self, role: str) -> None:\n        if role not in CARE_TEAM_REVIEW_ROLES:\n            raise PermissionError(\"This role is not allowed to review screening results.\")\n""", encoding="utf-8")'
```

---

# 9) Patch backend referrals service

## What this changes
- keeps referrals scoped to provider-family creators plus care coordinator/admin
- prevents accidental patient overexposure while making provider-family support explicit in messaging and checks

```bash
cd /home/trovas/Downloads/projects/byupw/block3_2026/BLISS/bliss-telehealth
python3 -c 'from pathlib import Path; Path("pbbf-api/app/modules/referrals/service.py").write_text("""from fastapi import HTTPException, status\nfrom sqlalchemy.orm import Session\n\nfrom app.db.models import Encounter, Patient, Referral, User\nfrom app.modules.audit.service import AuditService\nfrom app.modules.notifications.service import NotificationService\nfrom app.modules.referrals.repository import ReferralRepository\nfrom app.modules.referrals.schemas import (\n    ReferralCreateRequest,\n    ReferralStatus,\n    ReferralStatusUpdateRequest,\n)\n\nPROVIDER_FAMILY_ROLES = {\"provider\", \"counselor\", \"lactation_consultant\"}\nALLOWED_CREATE_ROLES = PROVIDER_FAMILY_ROLES | {\"care_coordinator\", \"admin\"}\nALLOWED_UPDATE_ROLES = ALLOWED_CREATE_ROLES\n\n\nclass ReferralService:\n    def __init__(self, db: Session):\n        self.db = db\n        self.repository = ReferralRepository(db)\n        self.audit_service = AuditService(db)\n        self.notification_service = NotificationService(db)\n\n    @staticmethod\n    def _role_name(user: User) -> str:\n        if hasattr(user, \"role_name\") and user.role_name:\n            return str(user.role_name).lower()\n        if hasattr(user, \"role\") and user.role and hasattr(user.role, \"name\"):\n            return str(user.role.name).lower()\n        return \"patient\"\n\n    def _require_role(self, actor: User, allowed: set[str]) -> None:\n        role_name = self._role_name(actor)\n        if role_name not in allowed:\n            raise HTTPException(\n                status_code=status.HTTP_403_FORBIDDEN,\n                detail=\"You are not allowed to perform this action.\",\n            )\n\n    def _ensure_patient_exists(self, patient_id: int) -> Patient:\n        patient = self.db.query(Patient).filter(Patient.id == patient_id).first()\n        if not patient:\n            raise HTTPException(\n                status_code=status.HTTP_404_NOT_FOUND,\n                detail=\"Patient not found.\",\n            )\n        return patient\n\n    def _ensure_encounter_exists(self, encounter_id: int | None) -> Encounter | None:\n        if not encounter_id:\n            return None\n        encounter = self.db.query(Encounter).filter(Encounter.id == encounter_id).first()\n        if not encounter:\n            raise HTTPException(\n                status_code=status.HTTP_404_NOT_FOUND,\n                detail=\"Encounter not found.\",\n            )\n        return encounter\n\n    def create_referral(self, payload: ReferralCreateRequest, actor: User) -> Referral:\n        self._require_role(actor, ALLOWED_CREATE_ROLES)\n\n        if not str(payload.destination_name or \"\").strip():\n            raise HTTPException(\n                status_code=status.HTTP_400_BAD_REQUEST,\n                detail=\"Referral destination is required.\",\n            )\n        if not str(payload.notes or \"\").strip():\n            raise HTTPException(\n                status_code=status.HTTP_400_BAD_REQUEST,\n                detail=\"Referral notes are required.\",\n            )\n\n        patient = self._ensure_patient_exists(payload.patient_id)\n        encounter = self._ensure_encounter_exists(payload.encounter_id)\n\n        if encounter is not None and encounter.patient_id != patient.id:\n            raise HTTPException(\n                status_code=status.HTTP_400_BAD_REQUEST,\n                detail=\"Encounter does not belong to the supplied patient.\",\n            )\n\n        referral = self.repository.create(\n            patient_id=patient.id,\n            encounter_id=encounter.id if encounter else None,\n            created_by_user_id=actor.id,\n            category=payload.category.value,\n            destination_name=payload.destination_name,\n            destination_contact=payload.destination_contact,\n            notes=payload.notes,\n            status=ReferralStatus.CREATED.value,\n            follow_up_at=payload.follow_up_at,\n        )\n\n        self.audit_service.log_event(\n            actor_user_id=actor.id,\n            action=\"referral.created\",\n            entity_type=\"referral\",\n            entity_id=referral.id,\n            details={\n                \"patient_id\": patient.id,\n                \"category\": referral.category,\n                \"status\": referral.status,\n                \"destination_name\": referral.destination_name,\n                \"actor_role\": self._role_name(actor),\n            },\n        )\n\n        self.notification_service.create_referral_follow_up_hook(referral=referral, actor=actor)\n        return referral\n\n    def get_referral(self, referral_id: int, actor: User) -> Referral:\n        referral = self.repository.get_by_id(referral_id)\n        if not referral:\n            raise HTTPException(\n                status_code=status.HTTP_404_NOT_FOUND,\n                detail=\"Referral not found.\",\n            )\n\n        role_name = self._role_name(actor)\n        if role_name == \"patient\":\n            patient_profile = getattr(actor, \"patient_profile\", None)\n            if not patient_profile or patient_profile.id != referral.patient_id:\n                raise HTTPException(\n                    status_code=status.HTTP_403_FORBIDDEN,\n                    detail=\"You are not allowed to view this referral.\",\n                )\n        return referral\n\n    def list_referrals(self, actor: User, patient_id: int | None = None, status_filter: str | None = None) -> list[Referral]:\n        role_name = self._role_name(actor)\n        if role_name == \"patient\":\n            patient_profile = getattr(actor, \"patient_profile\", None)\n            if not patient_profile:\n                return []\n            return self.repository.list_for_patient(patient_id=patient_profile.id, status_filter=status_filter)\n\n        if role_name in ALLOWED_CREATE_ROLES:\n            if patient_id:\n                return self.repository.list_for_patient(patient_id=patient_id, status_filter=status_filter)\n            return self.repository.list_all(status_filter=status_filter)\n\n        return []\n\n    def update_referral_status(self, referral_id: int, payload: ReferralStatusUpdateRequest, actor: User) -> Referral:\n        self._require_role(actor, ALLOWED_UPDATE_ROLES)\n        referral = self.repository.get_by_id(referral_id)\n        if not referral:\n            raise HTTPException(\n                status_code=status.HTTP_404_NOT_FOUND,\n                detail=\"Referral not found.\",\n            )\n\n        updated = self.repository.update_status(\n            referral=referral,\n            status_value=payload.status.value,\n            completion_outcome=payload.completion_outcome,\n            notes=payload.notes,\n        )\n\n        self.audit_service.log_event(\n            actor_user_id=actor.id,\n            action=\"referral.status_updated\",\n            entity_type=\"referral\",\n            entity_id=updated.id,\n            details={\n                \"status\": updated.status,\n                \"completion_outcome\": updated.completion_outcome,\n                \"actor_role\": self._role_name(actor),\n            },\n        )\n        return updated\n""", encoding="utf-8")'
```

---

# 10) Patch frontend appointments hook

## What this changes
- normalizes provider-family appointment records with patient/provider names from the enriched backend payload
- keeps patient booking/reschedule/cancel behavior intact
- makes provider-family pages more reliable when the backend returns richer appointment objects

```bash
cd /home/trovas/Downloads/projects/byupw/block3_2026/BLISS/bliss-telehealth
python3 -c 'from pathlib import Path; Path("pbbf-telehealth/src/modules/appointments/hooks/useAppointments.js").write_text("""import { useCallback, useEffect, useMemo, useState } from \"react\";\nimport {\n  cancelAppointmentRequest,\n  createAppointmentRequest,\n  listAppointmentsRequest,\n  rescheduleAppointmentRequest,\n} from \"../services/appointmentsApi\";\n\nconst DEFAULT_APPOINTMENT_DURATION_MINUTES = 60;\n\nfunction normalizeAppointment(raw) {\n  return {\n    id: raw?.id || raw?.appointment_id || \"temp-id\",\n    patientId: raw?.patient_id || raw?.patientId || null,\n    providerId: raw?.provider_id || raw?.providerId || null,\n    patientName: raw?.patient_name || raw?.patientName || \"Unknown patient\",\n    serviceType: raw?.service_type || raw?.serviceType || \"\",\n    visitReason: raw?.reason || raw?.visit_reason || raw?.visitReason || \"\",\n    scheduledAt:\n      raw?.start_at ||\n      raw?.scheduled_at ||\n      raw?.scheduledAt ||\n      raw?.appointment_time ||\n      \"\",\n    endAt: raw?.end_at || raw?.scheduled_end || raw?.endAt || \"\",\n    timezone: raw?.timezone || \"UTC\",\n    providerName: raw?.provider_name || raw?.providerName || \"To be assigned\",\n    status: raw?.status || \"booked\",\n    locationLabel: raw?.location_label || raw?.locationLabel || \"Virtual visit\",\n  };\n}\n\nfunction sortAppointmentsByDate(items) {\n  return [...items].sort((a, b) => new Date(a.scheduledAt) - new Date(b.scheduledAt));\n}\n\nfunction isUpcoming(appointment) {\n  const appointmentDate = new Date(appointment.scheduledAt);\n  return appointmentDate.getTime() >= Date.now();\n}\n\nfunction pad(value) {\n  return String(value).padStart(2, \"0\");\n}\n\nfunction toLocalDateTimeString(date) {\n  return [\n    date.getFullYear(),\n    \"-\",\n    pad(date.getMonth() + 1),\n    \"-\",\n    pad(date.getDate()),\n    \"T\",\n    pad(date.getHours()),\n    \":\",\n    pad(date.getMinutes()),\n    \":\",\n    pad(date.getSeconds()),\n  ].join(\"\");\n}\n\nfunction buildAppointmentWindow(scheduledAt) {\n  const start = new Date(scheduledAt);\n\n  if (Number.isNaN(start.getTime())) {\n    throw new Error(\"Please choose a valid appointment date and time.\");\n  }\n\n  const end = new Date(start.getTime() + DEFAULT_APPOINTMENT_DURATION_MINUTES * 60 * 1000);\n\n  return {\n    startAt: toLocalDateTimeString(start),\n    endAt: toLocalDateTimeString(end),\n  };\n}\n\nexport function useAppointments() {\n  const [appointments, setAppointments] = useState([]);\n  const [isLoading, setIsLoading] = useState(true);\n  const [isCreating, setIsCreating] = useState(false);\n  const [isUpdating, setIsUpdating] = useState(false);\n  const [loadError, setLoadError] = useState(\"\");\n  const [flashMessage, setFlashMessage] = useState(\"\");\n\n  const loadAppointments = useCallback(async () => {\n    try {\n      setIsLoading(true);\n      setLoadError(\"\");\n\n      const response = await listAppointmentsRequest();\n      const payload = response?.data || response;\n      const rows = payload?.items || payload?.appointments || payload || [];\n      const normalized = sortAppointmentsByDate(rows.map(normalizeAppointment));\n\n      setAppointments(normalized);\n    } catch (error) {\n      setLoadError(error?.message || \"Unable to load appointments.\");\n    } finally {\n      setIsLoading(false);\n    }\n  }, []);\n\n  useEffect(() => {\n    loadAppointments();\n  }, [loadAppointments]);\n\n  const upcomingAppointments = useMemo(() => {\n    return sortAppointmentsByDate(appointments.filter(isUpcoming));\n  }, [appointments]);\n\n  const nextAppointment = useMemo(() => {\n    return upcomingAppointments[0] || null;\n  }, [upcomingAppointments]);\n\n  async function createAppointment(values) {\n    try {\n      setIsCreating(true);\n      setFlashMessage(\"\");\n\n      const { startAt, endAt } = buildAppointmentWindow(values.scheduledAt);\n\n      const payload = {\n        provider_id: values.providerId || null,\n        service_type: values.serviceType,\n        reason: values.visitReason,\n        timezone: values.timezone,\n        start_at: startAt,\n        end_at: endAt,\n      };\n\n      const response = await createAppointmentRequest(payload);\n      await loadAppointments();\n\n      setFlashMessage(response?.message || \"Appointment request saved successfully.\");\n      return { success: true, response };\n    } catch (error) {\n      setFlashMessage(error?.message || \"Unable to create appointment.\");\n      return { success: false, error };\n    } finally {\n      setIsCreating(false);\n    }\n  }\n\n  async function rescheduleAppointment(appointmentId, scheduledAt) {\n    try {\n      setIsUpdating(true);\n      setFlashMessage(\"\");\n\n      const { startAt, endAt } = buildAppointmentWindow(scheduledAt);\n\n      const response = await rescheduleAppointmentRequest(appointmentId, {\n        start_at: startAt,\n        end_at: endAt,\n      });\n\n      await loadAppointments();\n      setFlashMessage(response?.message || \"Appointment rescheduled successfully.\");\n      return { success: true, response };\n    } catch (error) {\n      setFlashMessage(error?.message || \"Unable to reschedule appointment.\");\n      return { success: false, error };\n    } finally {\n      setIsUpdating(false);\n    }\n  }\n\n  async function cancelAppointment(appointmentId) {\n    try {\n      setIsUpdating(true);\n      setFlashMessage(\"\");\n\n      const response = await cancelAppointmentRequest(appointmentId, {\n        reason: \"Cancelled by patient from UI\",\n      });\n\n      await loadAppointments();\n      setFlashMessage(response?.message || \"Appointment cancelled successfully.\");\n      return { success: true, response };\n    } catch (error) {\n      setFlashMessage(error?.message || \"Unable to cancel appointment.\");\n      return { success: false, error };\n    } finally {\n      setIsUpdating(false);\n    }\n  }\n\n  return {\n    appointments,\n    upcomingAppointments,\n    nextAppointment,\n    isLoading,\n    isCreating,\n    isUpdating,\n    loadError,\n    flashMessage,\n    reload: loadAppointments,\n    createAppointment,\n    rescheduleAppointment,\n    cancelAppointment,\n  };\n}\n""", encoding="utf-8")'
```

---

# 11) Patch frontend provider dashboard

## What this changes
- updates wording so the provider-family dashboard is intentionally provider-family/coordinator aware
- makes the summary blocks tolerate richer appointment payloads more cleanly

```bash
cd /home/trovas/Downloads/projects/byupw/block3_2026/BLISS/bliss-telehealth
python3 -c 'from pathlib import Path; Path("pbbf-telehealth/src/pages/provider/Dashboard.jsx").write_text("""import { Link } from \"react-router-dom\";\n\nimport { ROUTES } from \"../../shared/constants/routes\";\nimport { useAppointments } from \"../../modules/appointments/hooks/useAppointments\";\nimport Loader from \"../../shared/components/Loader\";\nimport EmptyState from \"../../shared/components/EmptyState\";\nimport ErrorState from \"../../shared/components/ErrorState\";\n\nfunction normalizeProviderAppointment(raw) {\n  return {\n    id: raw?.id || raw?.appointment_id || \"temp-appointment-id\",\n    patientId: raw?.patient_id || raw?.patientId || null,\n    patientName: raw?.patient_name || raw?.patientName || \"Unknown patient\",\n    serviceType: raw?.service_type || raw?.serviceType || \"Visit\",\n    visitReason: raw?.visit_reason || raw?.visitReason || raw?.reason || \"\",\n    status: raw?.status || \"booked\",\n    scheduledAt: raw?.start_at || raw?.scheduled_at || raw?.scheduledAt || \"\",\n    screeningAlertLevel: raw?.screening_alert_level || raw?.screeningAlertLevel || \"none\",\n    screeningAlertText: raw?.screening_alert_text || raw?.screeningAlertText || \"\",\n    providerName: raw?.provider_name || raw?.providerName || \"\",\n  };\n}\n\nfunction formatDateTime(value) {\n  if (!value) return \"Not scheduled\";\n  const date = new Date(value);\n  if (Number.isNaN(date.getTime())) return value;\n  return new Intl.DateTimeFormat(undefined, { dateStyle: \"medium\", timeStyle: \"short\" }).format(date);\n}\n\nexport default function ProviderDashboard() {\n  const { appointments, isLoading, loadError } = useAppointments();\n\n  if (isLoading) {\n    return (\n      <div className=\"flex min-h-[40vh] items-center justify-center\">\n        <Loader label=\"Loading provider workspace...\" />\n      </div>\n    );\n  }\n\n  if (loadError) {\n    return <ErrorState title=\"Unable to load provider workspace\" message={loadError} />;\n  }\n\n  const normalized = appointments.map(normalizeProviderAppointment);\n  const upcoming = normalized.filter((item) => item.status !== \"cancelled\" && item.status !== \"completed\");\n  const highAlerts = normalized.filter((item) => item.screeningAlertLevel === \"high\");\n\n  return (\n    <div className=\"space-y-8\">\n      <section className=\"rounded-3xl bg-sky-900 px-8 py-8 text-white\">\n        <p className=\"text-sm uppercase tracking-[0.2em] text-sky-200\">Provider-family workspace</p>\n        <h1 className=\"mt-2 text-3xl font-semibold\">Clinical dashboard</h1>\n        <p className=\"mt-3 max-w-2xl text-sm leading-6 text-sky-100\">\n          Review assigned visits, identify screening alerts, document encounter notes, and manage referrals across the provider-family workflow.\n        </p>\n      </section>\n\n      <section className=\"grid gap-5 md:grid-cols-3\">\n        <div className=\"rounded-3xl border border-slate-200 bg-white p-6 shadow-sm\">\n          <p className=\"text-sm text-slate-500\">Assigned appointments</p>\n          <p className=\"mt-3 text-3xl font-semibold text-slate-900\">{normalized.length}</p>\n        </div>\n        <div className=\"rounded-3xl border border-slate-200 bg-white p-6 shadow-sm\">\n          <p className=\"text-sm text-slate-500\">Open workflow</p>\n          <p className=\"mt-3 text-3xl font-semibold text-slate-900\">{upcoming.length}</p>\n        </div>\n        <div className=\"rounded-3xl border border-slate-200 bg-white p-6 shadow-sm\">\n          <p className=\"text-sm text-slate-500\">Screening alerts</p>\n          <p className=\"mt-3 text-3xl font-semibold text-slate-900\">{highAlerts.length}</p>\n        </div>\n      </section>\n\n      <section className=\"space-y-4\">\n        <div className=\"flex flex-col gap-3 md:flex-row md:items-end md:justify-between\">\n          <div>\n            <h2 className=\"text-2xl font-semibold text-slate-900\">Upcoming appointments</h2>\n            <p className=\"mt-1 text-sm text-slate-600\">Prioritize visit preparation and review patient context before entering the encounter workspace.</p>\n          </div>\n          <Link\n            to={ROUTES.provider.notes}\n            className=\"inline-flex rounded-xl bg-sky-700 px-5 py-3 text-sm font-semibold text-white transition hover:bg-sky-800\"\n          >\n            Open notes workspace\n          </Link>\n        </div>\n\n        {!upcoming.length ? (\n          <EmptyState\n            title=\"No assigned appointments yet\"\n            message=\"There are no provider-family appointments visible right now.\"\n          />\n        ) : (\n          <div className=\"grid gap-4\">\n            {upcoming.slice(0, 6).map((appointment) => (\n              <article key={appointment.id} className=\"rounded-2xl border border-slate-200 bg-white p-5 shadow-sm\">\n                <div className=\"flex flex-col gap-3 md:flex-row md:items-center md:justify-between\">\n                  <div>\n                    <h3 className=\"text-lg font-semibold text-slate-900\">{appointment.patientName}</h3>\n                    <p className=\"mt-1 text-sm text-slate-600\">{appointment.serviceType}</p>\n                    <p className=\"mt-1 text-sm text-slate-600\">{formatDateTime(appointment.scheduledAt)}</p>\n                  </div>\n                  <div className=\"text-sm text-slate-600\">\n                    <p className=\"font-medium capitalize text-slate-900\">{appointment.status}</p>\n                    {appointment.screeningAlertLevel !== \"none\" ? (\n                      <p className=\"mt-1 text-amber-700\">\n                        Screening alert: {appointment.screeningAlertText || appointment.screeningAlertLevel}\n                      </p>\n                    ) : null}\n                  </div>\n                </div>\n              </article>\n            ))}\n          </div>\n        )}\n      </section>\n\n      <section className=\"grid gap-5 md:grid-cols-2\">\n        <Link to={ROUTES.provider.notes} className=\"rounded-3xl border border-slate-200 bg-white p-6 shadow-sm transition hover:border-sky-300\">\n          <h3 className=\"text-xl font-semibold text-slate-900\">Encounter notes</h3>\n          <p className=\"mt-2 text-sm leading-6 text-slate-600\">Document, update, and finalize encounter notes for the assigned clinical workflow.</p>\n        </Link>\n        <Link to={ROUTES.provider.referrals} className=\"rounded-3xl border border-slate-200 bg-white p-6 shadow-sm transition hover:border-sky-300\">\n          <h3 className=\"text-xl font-semibold text-slate-900\">Referrals</h3>\n          <p className=\"mt-2 text-sm leading-6 text-slate-600\">Create and track referrals where the provider-family or coordinator workflow requires follow-up support.</p>\n        </Link>\n      </section>\n    </div>\n  );\n}\n""", encoding="utf-8")'
```

---

# 12) Patch frontend provider notes page

## What this changes
- keeps notes available only to roles that the backend supports
- improves wording to reflect provider-family note access explicitly

```bash
cd /home/trovas/Downloads/projects/byupw/block3_2026/BLISS/bliss-telehealth
python3 -c 'from pathlib import Path; Path("pbbf-telehealth/src/pages/provider/Notes.jsx").write_text("""import { useMemo, useState } from \"react\";\n\nimport { useAppointments } from \"../../modules/appointments/hooks/useAppointments\";\nimport PatientSummaryCard from \"../../modules/encounters/components/PatientSummaryCard\";\nimport EncounterEditor from \"../../modules/encounters/components/EncounterEditor\";\nimport { useEncounterEditor } from \"../../modules/encounters/hooks/useEncounterEditor\";\nimport Loader from \"../../shared/components/Loader\";\nimport EmptyState from \"../../shared/components/EmptyState\";\nimport ErrorState from \"../../shared/components/ErrorState\";\n\nfunction normalizeProviderAppointment(raw) {\n  return {\n    id: raw?.id || raw?.appointment_id || \"temp-appointment-id\",\n    patientId: raw?.patient_id || raw?.patientId || null,\n    patientName: raw?.patient_name || raw?.patientName || \"Unknown patient\",\n    serviceType: raw?.service_type || raw?.serviceType || \"Visit\",\n    visitReason: raw?.visit_reason || raw?.visitReason || raw?.reason || \"\",\n    status: raw?.status || \"booked\",\n    screeningAlertLevel: raw?.screening_alert_level || raw?.screeningAlertLevel || \"none\",\n    screeningAlertText: raw?.screening_alert_text || raw?.screeningAlertText || \"\",\n    encounterId: raw?.encounter_id || raw?.encounterId || null,\n  };\n}\n\nexport default function ProviderNotesPage() {\n  const { appointments, isLoading, loadError } = useAppointments();\n  const normalizedAppointments = useMemo(() => appointments.map(normalizeProviderAppointment), [appointments]);\n  const [selectedAppointmentId, setSelectedAppointmentId] = useState(normalizedAppointments[0]?.id || \"\");\n\n  const selectedAppointment =\n    normalizedAppointments.find((item) => item.id === selectedAppointmentId) || normalizedAppointments[0] || null;\n\n  const {\n    values,\n    errors,\n    status,\n    isLoading: encounterLoading,\n    isSaving,\n    message,\n    loadError: encounterLoadError,\n    updateField,\n    saveDraft,\n    finalize,\n  } = useEncounterEditor(selectedAppointment?.id);\n\n  if (isLoading) {\n    return (\n      <div className=\"flex min-h-[40vh] items-center justify-center\">\n        <Loader label=\"Loading encounter workspace...\" />\n      </div>\n    );\n  }\n\n  if (loadError) {\n    return <ErrorState title=\"Unable to load encounter workspace\" message={loadError} />;\n  }\n\n  if (!normalizedAppointments.length) {\n    return (\n      <EmptyState\n        title=\"No appointment context available\"\n        message=\"No provider-family appointments are currently available for encounter documentation.\"\n      />\n    );\n  }\n\n  return (\n    <div className=\"space-y-8\">\n      <section className=\"rounded-3xl bg-sky-900 px-8 py-8 text-white\">\n        <p className=\"text-sm uppercase tracking-[0.2em] text-sky-200\">Provider-family notes</p>\n        <h1 className=\"mt-2 text-3xl font-semibold\">Encounter workspace</h1>\n        <p className=\"mt-3 max-w-2xl text-sm leading-6 text-sky-100\">\n          Select an appointment, review patient context, and complete structured clinical documentation for supported provider-family roles.\n        </p>\n      </section>\n\n      <section className=\"rounded-3xl border border-slate-200 bg-white p-6 shadow-sm\">\n        <label htmlFor=\"provider-appointment-select\" className=\"mb-2 block text-sm font-medium text-slate-700\">\n          Select appointment\n        </label>\n        <select\n          id=\"provider-appointment-select\"\n          value={selectedAppointment?.id || \"\"}\n          onChange={(event) => setSelectedAppointmentId(event.target.value)}\n          className=\"w-full rounded-xl border border-slate-300 px-4 py-3 outline-none transition focus:border-sky-500 focus:ring-2 focus:ring-sky-100\"\n        >\n          {normalizedAppointments.map((appointment) => (\n            <option key={appointment.id} value={appointment.id}>\n              {appointment.patientName} — {appointment.serviceType}\n            </option>\n          ))}\n        </select>\n      </section>\n\n      {encounterLoadError ? (\n        <ErrorState title=\"Unable to load encounter\" message={encounterLoadError} />\n      ) : encounterLoading ? (\n        <div className=\"flex min-h-[24vh] items-center justify-center\">\n          <Loader label=\"Loading encounter details...\" />\n        </div>\n      ) : (\n        <div className=\"grid gap-6 xl:grid-cols-[360px_minmax(0,1fr)]\">\n          <PatientSummaryCard appointment={selectedAppointment} />\n          <EncounterEditor\n            values={values}\n            errors={errors}\n            status={status}\n            message={message}\n            isSaving={isSaving}\n            onChange={updateField}\n            onSaveDraft={saveDraft}\n            onFinalize={finalize}\n          />\n        </div>\n      )}\n    </div>\n  );\n}\n""", encoding="utf-8")'
```

---

# 13) Patch frontend provider referrals page

## What this changes
- clarifies that provider-family and coordinator actions are intentionally separated
- keeps appointment-context selection intact while aligning wording with backend-supported actions

```bash
cd /home/trovas/Downloads/projects/byupw/block3_2026/BLISS/bliss-telehealth
python3 -c 'from pathlib import Path; Path("pbbf-telehealth/src/pages/provider/Referrals.jsx").write_text("""import { useMemo, useState } from \"react\";\n\nimport { useAppointments } from \"../../modules/appointments/hooks/useAppointments\";\nimport ReferralForm from \"../../modules/referrals/components/ReferralForm\";\nimport ReferralTimeline from \"../../modules/referrals/components/ReferralTimeline\";\nimport { useReferrals } from \"../../modules/referrals/hooks/useReferrals\";\nimport Loader from \"../../shared/components/Loader\";\nimport EmptyState from \"../../shared/components/EmptyState\";\nimport ErrorState from \"../../shared/components/ErrorState\";\n\nfunction normalizeProviderAppointment(raw) {\n  return {\n    id: raw?.id || raw?.appointment_id || \"temp-appointment-id\",\n    patientId: raw?.patient_id || raw?.patientId || null,\n    patientName: raw?.patient_name || raw?.patientName || \"Unknown patient\",\n    serviceType: raw?.service_type || raw?.serviceType || \"Visit\",\n    encounterId: raw?.encounter_id || raw?.encounterId || null,\n  };\n}\n\nexport default function ProviderReferralsPage() {\n  const { appointments, isLoading: appointmentsLoading, loadError: appointmentsError } = useAppointments();\n  const {\n    referrals,\n    isLoading: referralsLoading,\n    isSaving,\n    loadError: referralsError,\n    message,\n    createReferral,\n    updateStatus,\n  } = useReferrals();\n\n  const normalizedAppointments = useMemo(() => appointments.map(normalizeProviderAppointment), [appointments]);\n  const [selectedAppointmentId, setSelectedAppointmentId] = useState(normalizedAppointments[0]?.id || \"\");\n\n  const selectedAppointment =\n    normalizedAppointments.find((item) => item.id === selectedAppointmentId) || normalizedAppointments[0] || null;\n\n  if (appointmentsLoading || referralsLoading) {\n    return (\n      <div className=\"flex min-h-[40vh] items-center justify-center\">\n        <Loader label=\"Loading referrals workspace...\" />\n      </div>\n    );\n  }\n\n  if (appointmentsError) {\n    return <ErrorState title=\"Unable to load appointment context\" message={appointmentsError} />;\n  }\n\n  if (referralsError) {\n    return <ErrorState title=\"Unable to load referrals\" message={referralsError} />;\n  }\n\n  return (\n    <div className=\"space-y-8\">\n      <section className=\"rounded-3xl bg-sky-900 px-8 py-8 text-white\">\n        <p className=\"text-sm uppercase tracking-[0.2em] text-sky-200\">Provider / coordinator referrals</p>\n        <h1 className=\"mt-2 text-3xl font-semibold\">Referral management</h1>\n        <p className=\"mt-3 max-w-2xl text-sm leading-6 text-sky-100\">\n          Create referrals for follow-up support and track referral progress over time, using only the actions allowed for the current provider-family or coordinator role.\n        </p>\n      </section>\n\n      {!normalizedAppointments.length ? (\n        <EmptyState\n          title=\"No appointment context available\"\n          message=\"No provider-family appointment context is currently available for referral creation.\"\n        />\n      ) : (\n        <section className=\"rounded-3xl border border-slate-200 bg-white p-6 shadow-sm\">\n          <label htmlFor=\"referral-appointment-select\" className=\"mb-2 block text-sm font-medium text-slate-700\">\n            Select appointment / patient context\n          </label>\n          <select\n            id=\"referral-appointment-select\"\n            value={selectedAppointment?.id || \"\"}\n            onChange={(event) => setSelectedAppointmentId(event.target.value)}\n            className=\"w-full rounded-xl border border-slate-300 px-4 py-3 outline-none transition focus:border-sky-500 focus:ring-2 focus:ring-sky-100\"\n          >\n            {normalizedAppointments.map((appointment) => (\n              <option key={appointment.id} value={appointment.id}>\n                {appointment.patientName} — {appointment.serviceType}\n              </option>\n            ))}\n          </select>\n        </section>\n      )}\n\n      <div className=\"grid gap-6 xl:grid-cols-[minmax(0,420px)_minmax(0,1fr)]\">\n        <ReferralForm\n          appointment={selectedAppointment}\n          isSaving={isSaving}\n          message={message}\n          onSubmit={createReferral}\n        />\n        <ReferralTimeline\n          referrals={referrals}\n          onStatusChange={updateStatus}\n          isSaving={isSaving}\n        />\n      </div>\n    </div>\n  );\n}\n""", encoding="utf-8")'
```

---

# 14) Inspect stale provider-family references in tests before editing them

## Why this step exists
Phase 3 changes backend + provider-family behavior. Before updating any tests, inspect what assertions already exist so you don’t patch them blindly.

```bash
cd /home/trovas/Downloads/projects/byupw/block3_2026/BLISS/bliss-telehealth

find pbbf-telehealth/src/pages/provider -type f 2>/dev/null | sort | xargs -I{} sh -c 'echo "----- {} -----"; sed -n "1,260p" "{}"; echo'

find pbbf-telehealth/src/modules/appointments/__tests__ -type f 2>/dev/null | sort | xargs -I{} sh -c 'echo "----- {} -----"; sed -n "1,260p" "{}"; echo'

find pbbf-telehealth/src/modules/auth/__tests__ -type f 2>/dev/null | sort | xargs -I{} sh -c 'echo "----- {} -----"; sed -n "1,260p" "{}"; echo'

find pbbf-api/tests -type f 2>/dev/null | grep -E 'appointments|telehealth|encounters|screenings|referrals|permissions' | sort | xargs -I{} sh -c 'echo "----- {} -----"; sed -n "1,260p" "{}"; echo'
```

### Fast grep for role strings likely affected by Phase 3

```bash
cd /home/trovas/Downloads/projects/byupw/block3_2026/BLISS/bliss-telehealth

grep -R -n "counselor\|lactation_consultant\|care_coordinator\|provider-family\|provider family" pbbf-api pbbf-telehealth/src 2>/dev/null
```

---

# 15) Validate the patched known files

## Backend syntax check

```bash
cd /home/trovas/Downloads/projects/byupw/block3_2026/BLISS/bliss-telehealth/pbbf-api
source .venv/bin/activate
python -m py_compile \
  app/modules/appointments/service.py \
  app/modules/appointments/repository.py \
  app/modules/telehealth/service.py \
  app/modules/encounters/service.py \
  app/modules/screenings/service.py \
  app/modules/referrals/service.py
```

## Frontend build

```bash
cd /home/trovas/Downloads/projects/byupw/block3_2026/BLISS/bliss-telehealth/pbbf-telehealth
npm run build
```

---

# 16) Manual verification checklist for Phase 3

```text
[ ] provider can still view provider dashboard, notes, and referrals.
[ ] counselor can load provider-family appointment context without backend denial.
[ ] lactation consultant can load provider-family appointment context without backend denial.
[ ] care coordinator can still access provider-lite dashboard/referrals paths intended for coordination work.
[ ] patient still cannot access provider/admin workflows.
[ ] admin still has full operational visibility.
[ ] provider-family encounter access is still limited to assigned appointment context.
[ ] patient self-ownership still governs patient appointment cancel/reschedule behavior.
```

---

# 17) Stop point before patching the remaining unknown Phase 3 files

After you run the **inspection-only** commands for:
- `pbbf-api/app/common/permissions/object_access.py`
- `pbbf-api/app/modules/telehealth/repository.py`
- auth/users dependency helpers

paste those outputs back before touching them. Those files are part of full Phase 3 normalization, but their structures were not yet safely known when this command pack was generated. citeturn76search7turn77search7turn79search1turn83search1
